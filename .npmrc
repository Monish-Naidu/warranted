# React Native's Metro bundler cannot follow pnpm's symlinked store, so the
# workspace uses npm-style hoisting. Without this, `expo start` fails to
# resolve react-native's own transitive dependencies.
node-linker=hoisted
