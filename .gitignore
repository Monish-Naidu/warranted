node_modules/
dist/
build/
.expo/
.turbo/
*.tsbuildinfo

# env — never commit real credentials
.env
.env.*
!.env.example

# uploads (local dev photo storage)
apps/api/uploads/

# native build output
apps/mobile/ios/
apps/mobile/android/

# os / editor
.DS_Store
*.log
.vscode/
.idea/

# claude local overrides
.claude/settings.local.json
.postgres/

# vercel cli local state (added by `vercel link`). Deliberately not the
# broader `.env*` the CLI appends — that would re-ignore .env.example, which
# is tracked on purpose.
.vercel/
